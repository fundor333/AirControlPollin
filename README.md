# Air Controll and Pollin

If you want to use it you need to fill the .env file with the Google Map Api Token and after lanch ```make install``` having poetry install.

After that ```make run``` will run the code
